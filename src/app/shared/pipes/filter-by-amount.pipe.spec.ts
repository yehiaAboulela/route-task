import { FilterByAmountPipe } from './filter-by-amount.pipe';

describe('FilterByAmountPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterByAmountPipe();
    expect(pipe).toBeTruthy();
  });
});
